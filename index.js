const express = require('express');
const app = express();

// 호스팅 서버가 뚫어준 문(포트) 번호로 정확히 맞춰주기
const port = process.env.SERVER_PORT || 26937;

app.get('/', (req, res) => {
  res.send('환영합니다! 나의 첫 번째 Node.js 서버가 드디어 인터넷에 연결되었습니다! 🚀🎉');
});

// '0.0.0.0'을 꼭 넣어줘야 밖에서(외부 인터넷) 들어올 수 있어!
app.listen(port, '0.0.0.0', () => {
  console.log(`서버가 완벽하게 켜졌습니다! 할당된 포트: ${port}`);
});